clear all
close all
clear all
close all
% import data
% choose between:
% folder -> (assignment, robot-sim)
% arena -> (1-orignal_arnea, 2-fast_arena)
folder1 = 'robot-sim';
arena1 = '2-fast_arena';

textFileData = readtable(['../../', num2str(folder1), '/stats/', num2str(arena1), '/distance_travelled_', num2str(folder1), '.txt']);
arrayData = textFileData(:,5);
distances = table2array(arrayData);
% test
[p,h] = ranksum(x,y);